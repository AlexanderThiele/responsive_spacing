name: example
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.17.5 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  responsive_spacing: ^0.2.1
    #path: ../
  flutter_lints: ^2.0.1

flutter:
  uses-material-design: true
